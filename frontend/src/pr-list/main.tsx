import React from "react";
import ReactDOM from "react-dom/client";
import { PrListApp } from "./PrListApp";
import "./pr-list.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PrListApp />
  </React.StrictMode>,
);
